# 前言

> **本讲目标：** 学会用 `@` 精准控制 AI 看什么、不看什么，掌握“最小上下文”思路，减少跑偏与上下文污染。
> 
> **预计时长：** 75 分钟
> 
> **难度等级：** 技能

## 自学导航卡

- **前置依赖：** 已掌握 Rules 基础，能引用文件与目录
- **预计耗时：** 新手 75-105 分钟｜有经验 50-75 分钟
- **完成标准：** 用 3-5 个文件完成一次“类型→API→Hook→页面”的分层生成
- **卡点入口：** 若输出混乱，立刻缩减上下文并重开单任务对话

# 核心认知：AI 的输出质量 = 上下文质量

## 一、为什么上下文如此重要？

AI 不是全知全能的。它的每一次回答都 **完全基于你给它的信息**。

```
信息不足 → AI 瞎猜 → 生成的代码和你的项目格格不入
信息过多 → AI 混乱 → 抓不住重点，输出质量下降
信息精准 → AI 高效 → 生成的代码直接可用
```

## 二、上下文的"金发姑娘原则"（Goldilocks Principle）

![[Pasted image 20260901092415.png]]

## 三、上下文的四个层级

|层级|来源|特点|控制方式|
|---|---|---|---|
|**系统层**|Rules 文件|自动加载，持久生效|编辑 `.codebuddy/rules/`|
|**会话层**|当前对话历史|随对话积累|开新对话清空|
|**指令层**|`@` 引用|精准注入|主动选择|
|**隐式层**|当前打开的文件|自动感知|切换/关闭文件|

# 核心工具：`@` 符号全家桶

**此操作一定要动手打出 @ 符号再引用需要的内容，否则粘贴下述示例将 @ 无法生效**

## 一、`@Files` —— 精准引用文件

**用途：** 让 AI 阅读指定文件的完整内容。

**语法：**

```
@Files src/components/UserCard.tsx
```

**针对这次实操的项目在 `practice-project` 文件夹下**

**使用**：

**方式一：在输入框中输入 @**

1. 在对话框中输入 `@`
2. 在弹出的列表中选择文件
3. 确认引用后再发送指令

![[Pasted image 20260901092647.png]]

**方式二：点击输入框上方的 @ 按钮**

1. 点击对话框上侧的 `@` 按钮
2. 在文件选择器中选择目标文件
3. 完成后继续描述需求

![[Pasted image 20260901092705.png]]

**典型场景：**

```
请参考 @Files src/components/UserCard.tsx 的风格，
帮我创建一个 ProductCard 组件，展示商品图片、名称、价格和评分。
```
![[Pasted image 20260901092744.png]]
这里作为第一个示例，将使用视频展示如何操作，后续将不再展示视频，而是直接展示图片

**进阶用法 —— 多文件引用：**

```
请阅读以下文件：
@Files src/types/user.ts
@Files src/types/product.ts

然后创建一个 OrderType，关联 User 和 Product。
```

**注意事项：**

- 每次对话引用的文件不要超过 5 个，否则上下文过长
- 优先引用 **类型定义文件** 和 **接口文件**，而不是实现文件

## 二、`@Folders` —— 引用目录结构

**用途：** 让 AI 了解项目目录结构，而不需要阅读每个文件。

**语法：**

```
@Folders components
```

**典型场景：**

```
看一下 @Folders components 的目录结构，
我想新增一个 Dashboard 模块，请规划它的文件结构，与现有结构保持一致。
```

**适用于：**

- 规划新功能的文件组织
- 了解项目整体架构
- 确保新代码放在正确的位置

**示例运行效果图：**

规划文件结构效果截图：

![[Pasted image 20260901092817.png]]

从结果可以看到，AI 会先读取项目结构，再基于现有目录规划新模块的文件布局，并说明各核心文件的职责。你需要先审阅该规划，确认符合预期后再执行；如果不满意，可先调整需求或修改规划，再继续运行。

按规划创建效果截图：

![[Pasted image 20260901092827.png]]

## 三、`@Terminal` —— 引用终端输出

**用途：** 将终端的报错信息、日志等直接传递给 AI。

**典型场景：**

```
运行 npm run build 后出现以下错误：
@终端

请帮我分析原因并修复。
```

**最佳实践：**

- 只引用 **相关的** 终端输出，不要引用整个历史
- 报错信息 + 触发命令一起提供
- 如果错误信息很长，可以只截取关键部分

**示例操作图片：**

![[Pasted image 20260901092841.png]]

## 四、`@Docs` —— 引用文档

**用途：** 让 AI 参考项目文档或外部文档。

**典型场景：**

```
根据 @Docs 中定义的接口规范，
帮我实现前端的数据获取层，使用 useSWR。
```

**示例操作图片（点击 Docs 后一定还要选择具体文档）：**

![[Pasted image 20260901092851.png]]

## 五、`@Code` —— 引用代码片段

**用途：** 选中编辑器中的代码片段，精确引用。

**操作方式：**

![[Pasted image 20260901092904.png]]

**典型场景：**

```
@Code 片段
请解释这段代码的逻辑。
```

**示例结果图片：**

![[Pasted image 20260901092914.png]]

# 实操：避免"上下文污染"

## 一、什么是上下文污染？

当你给 AI 的信息中包含了 **不相关** 或 **误导性** 的内容时，AI 的输出质量会急剧下降。

**常见污染场景：**

|污染类型|描述|后果|
|---|---|---|
|**陈旧信息**|引用了已经废弃的旧代码|AI 生成兼容旧代码的方案|
|**噪声过多**|把整个 500 行文件都扔给 AI|AI 抓不住重点|
|**矛盾信息**|对话前面说"用 REST"，后面又说"用 GraphQL"|AI 左右为难|
|**跨任务污染**|一个对话窗口讨论了 3 个不同的功能|AI 把功能搞混|

## 二、最小上下文原则（Minimum Context Principle）

> **只给 AI 看它完成当前任务所需的最少信息。**

### 实操对比

**不推荐示范：上下文过载**

```
帮我写一个搜索功能。

@Files src/components/Header.tsx
@Files src/components/Footer.tsx
@Files src/components/Sidebar.tsx
@Files src/components/UserProfile.tsx
@Files src/components/SearchBar.tsx
@Files src/lib/api.ts
@Files src/types/index.ts
@Files src/hooks/useAuth.ts
@Files src/hooks/useSearch.ts
@Files src/styles/globals.css
```

**推荐示范：精准上下文**

```
帮我完善搜索功能，要求：
1. 支持防抖（300ms）
2. 搜索结果高亮匹配关键词
3. 支持键盘导航（上下箭头 + Enter）

参考现有代码：
@Files src/components/SearchBar.tsx     ← 现有搜索组件
@Files src/hooks/useSearch.ts           ← 现有搜索 Hook
@Files src/types/index.ts              ← 类型定义（只看 SearchResult 类型）
```

## 三、上下文管理的黄金法则

![[Pasted image 20260901092952.png]]

# 案例实战：让 AI 通过阅读 API 文档，自动实现前端对接

## 一、场景设定

假设后端同事给了你一份 API 文档，你需要在前端实现完整的对接。

**针对这次实操的项目在 `api-practice-project` 文件夹下**

## 二、Step 1：准备 API 文档

首先创建 API 文档文件 `docs/api-spec.md`：

详细文件内容

`# 用户管理 API ## 获取用户列表 - GET /api/users - Query: page(number), pageSize(number), keyword(string) - Response: { code: 200, data: { list: User[], total: number }, message: "success" } ## 获取用户详情 - GET /api/users/:id - Response: { code: 200, data: User, message: "success" } ## 创建用户 - POST /api/users - Body: { name: string, email: string, role: "admin" | "user" } - Response: { code: 200, data: User, message: "success" } ## 更新用户 - PUT /api/users/:id - Body: Partial<User> - Response: { code: 200, data: User, message: "success" } ## 删除用户 - DELETE /api/users/:id - Response: { code: 200, data: null, message: "success" } ## User 类型 - id: number - name: string - email: string - role: "admin" | "user" - avatar: string - createdAt: string (ISO 8601)`

## 三、Step 2：让 AI 阅读文档并生成类型

```
阅读 @Files docs/api-spec.md，然后帮我：
1. 在 src/types/user.ts 中定义完整的 TypeScript 类型
2. 包含 API 请求参数和响应的类型
3. 包含通用的 API 响应包装类型
```

## 四、Step 3：让 AI 生成 API 请求层

```
基于 @Files src/types/user.ts 中的类型定义，
在 src/lib/api/user.ts 中创建完整的 API 请求函数：
1. 使用 fetch 封装
2. 统一错误处理
3. 支持 TypeScript 类型推导
4. baseURL 从环境变量读取
```

## 五、Step 4：让 AI 生成自定义 Hook

```
基于 @Files src/lib/api/user.ts 中的 API 函数，
创建以下 Hook：
1. useUsers(params) —— 获取用户列表，支持分页和搜索
2. useUser(id) —— 获取单个用户详情
3. useCreateUser() —— 创建用户（包含 loading 和 error 状态）
4. useUpdateUser() —— 更新用户
5. useDeleteUser() —— 删除用户（带确认）

使用 useSWR 实现数据获取和缓存。
```

## 六、Step 5：让 AI 生成页面组件

```
基于 @Files src/hooks/useUsers.ts 中的 Hook，
创建一个用户管理页面，包含：
1. 顶部搜索栏 + "新增用户"按钮
2. 用户列表表格（姓名、邮箱、角色、创建时间、操作）
3. 分页控件
4. 新增/编辑用户的弹窗表单
5. 删除确认弹窗

使用 TailwindCSS 样式，参考 Linear 的设计风格。
```

## 七、Step 6：运行项目

```
运行项目，先检查环境是否适配，不适合的话先安装适配依赖。
```

**⚠️ 警告提示：** 由于模型给出的代码并不完全一致，所以当在运行项目时出现错误，请再次让 AI 检查。可将报错界面截图，发送给模型 Kimi-K2.5 修复。

## 八、关键总结

注意每一步我们是如何 **分层构建上下文** 的：

```
API 文档（源头）
    ↓ 引用文档
类型定义（数据结构）
    ↓ 引用类型文件
API 请求函数（网络层）
    ↓ 引用 API 文件
自定义 Hook（状态层）
    ↓ 引用 Hook 文件
页面组件（视图层）
```

每一步只引用 **上一步的产出**，而不是把所有东西一股脑扔给 AI。这就是 **分层上下文投喂** 的精髓。

如遇到不知如何下手的时候，可以观看如下完整实操视频：


# 进阶技巧

## 一、上下文"预热"技巧

在正式提需求之前，先让 AI "认识"你的项目。

**适用场景**

- 你刚切到一个新项目
- 你要连续做 2-3 个相关功能
- 你希望 AI 输出风格与现有代码保持一致

**错误用法 vs 正确用法**

**错误用法（直接开干）：**

`帮我做一个用户列表页面。`

**正确用法（先预热再提需求）：**

`我先让你了解一下项目结构： @Folders src  目前项目使用 Next.js 14 + TailwindCSS， 状态管理用 Zustand，数据获取用 useSWR。  接下来我会让你帮我实现几个功能， 请在后续的回答中保持与项目现有模式的一致性。`

**实际案例**

- 需求：新增“用户搜索 + 分页”
- 未预热结果：AI 可能改用 React Query、改造目录结构
- 预热后结果：更容易沿用 `useSWR + Zustand` 现有模式，返工更少

## 二、上下文"锚点"技巧

当对话变长时，用"锚点"帮 AI 回忆关键信息。

**适用场景**

- 会话已经超过 10 轮
- 中间切过多个任务（列表、详情、导出）
- 你发现 AI 开始“忘记”既定类型或文件位置

**错误用法 vs 正确用法**

**错误用法（不回顾上下文）：**

`现在加导出 CSV。`

**正确用法（先锚点再继续）：**

`（接上面的用户管理功能） 回顾一下： - 我们用的是 @Files src/types/user.ts 中的 User 类型 - API 层在 @Files src/lib/api/user.ts 现在帮我给用户列表添加"导出 CSV"功能。`

**实际案例**

- 症状：AI 把导出字段写成 `user_name`，而现有类型是 `name`
- 加锚点后：AI 会优先复用 `User` 类型，字段漂移明显减少

## 三、负面上下文（告诉 AI "不要什么"）

有时候告诉 AI "不要做什么" 比 "要做什么" 更有效。

**适用场景**

- 你只想局部重构，不想改外部行为
- 项目已上线，接口与样式不能被破坏
- 团队规范明确限制依赖与命名

**错误用法 vs 正确用法**

**错误用法（目标宽泛，边界缺失）：**

`帮我优化这个组件。`

**正确用法（明确禁止项）：**

`帮我重构这个组件： @Files src/components/Dashboard.tsx  要求： - 不要改变任何对外的 Props 接口 - 不要引入新的第三方库 - 不要修改现有的 CSS 类名 - 只拆分内部逻辑，提取子组件`

**实际案例**

- 未给禁止项：AI 可能顺手改 Props、替换样式体系
- 给出禁止项后：输出会聚焦在“内部拆分与可读性优化”，可直接合并概率更高

# 课后练习

## 练习 1：API 对接实战（必做）

1. 找一个公开 API（如 JSONPlaceholder、GitHub API）
2. 把 API 文档喂给 CodeBuddy
3. 用分层上下文法，逐步生成：类型 → API 函数 → Hook → 页面
4. 记录每一步你给了哪些上下文

## 练习 2：上下文污染实验（推荐）

1. 故意给 AI 过多无关文件，观察输出质量
2. 然后精简到最少文件，对比输出质量
3. 记录两次的差异

## 练习 3：整理你的上下文策略（必做）

为你最常做的 3 种开发任务，各设计一套上下文投喂策略：

- 需要引用哪些文件？
- 按什么顺序提供？
- 哪些信息需要排除？

# 本讲小结

本讲结束时，你应该能做到：

- 熟练使用 @Files / @Folders / @Terminal / @Docs
- 理解并能应用“最小上下文原则”
- 能按“分层投喂”的顺序组织信息，提高稳定性
- 能识别常见的上下文污染并做排查
- 用一套清晰的上下文包完成一次 API 对接练习

## 核心公式

> **AI 输出质量 = f(上下文精准度)**
> 
> 上下文精准度 = 相关性 × (1 / 噪声量)

## 下一讲预告

> **第 4 讲：Inline Edit 与快速迭代 —— 心流不被打断**
> 
> 上下文解决了"AI 看什么"的问题，下一讲解决"怎么快速改"的问题。你将掌握 Cmd+K 行内编辑，实现真正的心流编程。