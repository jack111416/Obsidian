### xinference安装成功后的下一步

Anaconda 装好后，继续在刚才的 CMD 终端中执行：



cmd

```
# 创建 xinference 虚拟环境
conda create -n xinference_env python=3.11 -y

# 激活环境
conda activate xinference_env

# 继续安装 Xinference
```

conda install pytorch torchvision torchaudio pytorch-cuda=12.1 -c pytorch -c nvidia

```
python -c "import torch; print(f'CUDA 可用: {torch.cuda.is_available()}'); print(f'CUDA 版本: {torch.version.cuda}'); print(f'显卡数量: {torch.cuda.device_count()}')"
```

pip install llama-cpp-python==0.2.64 --upgrade --force-reinstall -i https://pypi.org/simple/

python -c "import llama_cpp; print('安装成功！版本：', llama_cpp.__version__)"

pip install "xinference[all]" llama-cpp-python==0.2.64 -i https://pypi.org/simple/

### 方法一：设置 CUDA_VISIBLE_DEVICES（推荐）

cmd

```
# 设置只使用第 0 块 GPU
set CUDA_VISIBLE_DEVICES=0

# 然后启动 Xinference
xinference-local --host 127.0.0.1 --port 9997
```



### 方法二：一行命令启动

cmd

```
set XINFERENCE_HOME=E:\xinference_data
set CUDA_VISIBLE_DEVICES=0 && xinference-local --host 127.0.0.1 --port 9997
```

```
set XINFERENCE_HOME=E:\xinference_data
setx XINFERENCE_HOME "E:\xinference_data" /M
```







https://gitee.com/dmg666/openclaw-termux-installer

curl -L -O https://gitee.com/dmg666/openclaw-termux-installer/raw/master/2.sh

dash 2.sh

```
# 启动
~/start-openclaw.sh
```



https://github.com/AlexAnys/awesome-openclaw-usecases-zh

https://github.com/jnMetaCode/agency-agents-zh

sulada-knowledge-base

openclaw skills install skill-vetter

- summarize: 文本摘要，可能需要本地算法或 API（例如 OpenAI）

- openai-whisper: OpenAI 的 Whisper 语音识别，需要 OpenAI API key

- nano-pdf: 编辑 PDF，可能本地工具 (poppler) 或 API

- nano-banana-pro: 图像生成，可能需要 DALL-E/Fal API

- github: 与 GitHub 交互，需要 GitHub token

- humanizer: 改写文本以更人性化，可能需要 LLM API

- proactive-agent: 代理主动行为，可能不需要外部 API，但需要配置

- obsidian: 管理 Obsidian 笔记库，本地文件访问，不需要 API

- memory-setup: 初始化记忆系统，可能不需要 API

- skill-vetter: 评估技能质量，可能需要 LLM API
- ***\*Markdown Converter ，Web Content Fetcher，Self-Improving Agent\****

=====================



![image-20260528160336232](C:\Users\Administrator\AppData\Roaming\Typora\typora-user-images\image-20260528160336232.png)



完整配置块(建议用于生产环境)

```
agents
defaults"
compaction'
modesafeguard'
reserveTokensFloor40000,
memoryFlush
enabled": true,
启用安全压缩模式
保留40k token给系统固定内容
开启主动记忆冲刷
softThresholdTokens":89600,使用 89.6k时触发冲刷(假设模型上限 128k)systemPrompt":"即将达到上下文限制，请将重要信息写入长期记忆。prompt”:"将重要的对话摘要记录到memory 文件，如果无需记录则回复NO_REPLY。
```



如果你希望Agent能智能检索，不仅记住项目细节，还能连接不同的知识点，可以考虑安装一个上下文引擎插件。

安装Lossless Claw (LCM)这个官方推荐的插件：

- **它能做什么**：LCM会将所有对话存入本地数据库（SQLite），并在上下文爆炸时自动压缩旧消息生成摘要，而非直接丢弃。切换会话后，它可以在后台搜索相关记忆。它会让你感觉Agent真的“记住”了之前的项目。
- **注意**：开启LCM会稍微增加一些Token消耗和存储占用。

#### 第一步：一键安装插件

在终端中执行以下命令，OpenClaw 会自动从官方插件市场（ClawHub）下载并安装 Lossless Claw 插件。

bash

```
openclaw plugins install @martian-engineering/lossless-claw
```



> 你可能会看到一些关于代码分析的安全提示，这通常是插件市场对新插件的标准检查流程，可以忽略，直接继续。

#### 第二步：激活插件

插件安装后，默认的上下文管理引擎（Context Engine）尚未启用。我们需要通过一个命令来激活它，这是**最重要的一步**，否则插件不会生效。

bash

```
openclaw config set plugins.slots.contextEngine lossless-claw
```



这个命令的作用，相当于告诉 OpenClaw：“请把处理对话记忆的模块，换成新安装的 Lossless Claw。”

#### 第三步：重启与验证

为了让所有更改生效，**必须重启 Gateway 服务**。

1. **重启 OpenClaw Gateway**：

   bash

   ```
   openclaw gateway restart
   ```

   

   d3485c189d7ea376654faf9a366a32ef5b9f2d335b259475bb268cdc092e41b8

   

   如果你当前在前台运行着 Gateway（能看到实时日志），可以按 `Ctrl+C` 停止它，然后重新运行 `openclaw gateway start`。

2. **验证安装是否成功**：
   执行以下命令，确认插件已正确加载：

   bash

   ```
   openclaw plugins list
   ```

   

   你应该能在输出的列表中看到 `lossless-claw`，并且其状态显示为 `enabled`，这表示安装成功

### 推荐配置（锦上添花）

为了让 LCM 在你的日常使用中表现得更聪明、更经济，官方推荐进行以下配置。这些配置通过环境变量来设置，你可以将它们添加到系统环境变量中，或者在启动 Gateway 前在终端中执行。

打开你的终端，依次执行以下三个命令：

- **设置保留的原始消息数量**：

  bash

  ```
  export LCM_FRESH_TAIL_COUNT=32
  ```

  

  这个变量决定了每次对话开始时，有多少条最新的消息**不进行压缩，保持原样**加载到上下文中。32 条是一个黄金平衡点，能确保你获得流畅的对话体验，不会让 AI 变成“金鱼脑”，忘记刚才聊了什么。

- **设置上下文触发阈值**：

  bash

  ```
  export LCM_CONTEXT_THRESHOLD=0.75
  ```

  

  这个变量是启动压缩的“开关”。当当前上下文窗口的使用率达到 **75%** 时，LCM 便会自动触发压缩机制，将一些“久远”的对话归档成摘要，为新的对话腾出空间，确保聊天永不中断。

- **设置无限层级压缩**：

  bash

  ```
  export LCM_INCREMENTAL_MAX_DEPTH=-1
  ```

  

  这个变量控制摘要可以“压缩再压缩”的深度。设置为 `-1` 意味着不设上限，理论上可以管理无限长的对话历史。对于需要长期维护的项目、持续的研发调试或构建个人知识库来说，这个设置非常实用。

> **提示**：如果你使用的是 Windows 系统，`export` 命令可能会报错。你可以将变量添加到 Windows 的系统环境变量中，或者在每次启动 Gateway 前使用 `set` 命令来代替。例如：
>
> cmd
>
> ```
> set LCM_FRESH_TAIL_COUNT=32
> ```
>
> 
>
> 如果你希望这些设置长期生效，建议将它们写入 OpenClaw 的配置文件或系统的环境变量中。



用 OpenClaw 开发一套智能获客智能体，本质上是搭建一个能 7x24 小时自主工作的“数字员工”团队。结合你上一轮多 Agent 的讨论基础，以下是完整的解决方案，从一个核心逻辑到落地的实战步骤。

------

## 一、为什么用 OpenClaw 做智能获客？

传统获客依赖人工，存在三大痛点：7x24 小时无人值守，造成大量意向客户流失（尤其在夜间、凌晨等非工作时段的咨询，往往无人响应）；纯云端 AI 存安全隐患；纯本地部署则模型迭代困难。

OpenClaw 开源框架采用“本地筑基 + 云端赋能”的混合架构，核心获客业务放在本地服务器，保障数据安全不泄露，同时可通过云端下发模型更新和获客策略。

**OpenClaw 获客智能体的核心循环：**

text

```
线索来源（微信/企微/邮件/表单） → AI 意向识别 → 自动分层 → 个性化跟进 → 转人工或成交
              ↑                                                   ↓
              └────────── 持续学习 & 迭代优化（云端策略更新）←────────┘
```



------

## 二、四步落地智能获客智能体

要搭建一个核心的获客智能体，通常需要下面四步。这和大语言模型本身做意图识别、工具调用的逻辑是一致的，OpenClaw 正是通过这种标准化方式，把“思路”变成“手脚”。

| 步骤       | 核心动作                         | 时间预估       | 技术要点                       |
| :--------- | :------------------------------- | :------------- | :----------------------------- |
| **Step 1** | 部署运行环境                     | 30分钟 - 2小时 | 云服务器选型 + 模型 API 配置   |
| **Step 2** | 创建多个获客 Agent（军团化分工） | 1小时          | `openclaw agents add` 分工隔离 |
| **Step 3** | 连接获客渠道（核心触达）         | 30分钟         | 绑定企业微信/飞书/WhatsApp     |
| **Step 4** | 开发获客专属 Skill（自定义能力） | 2-4小时        | 编写 `SKILL.md` + 脚本实现     |

下面逐一展开。

### Step 1：服务器选型与部署

为了支持 7x24 小时稳定运行，需要一台**云服务器**作为永远在线的“指挥部”。

- **选型建议**：腾讯云/华为云的 OpenClaw 应用镜像是最快捷的方案，均已预装 OpenClaw。推荐最低配置：2 核 CPU、4GB 内存、50GB 系统盘。
- **部署操作**：
  - 进入云服务商控制台 → 选择应用镜像 → 筛选 OpenClaw (Clawdbot) → 购买实例。
  - 实例创建后，在“应用管理”页面配置 LLM 的 API Key（支持 DeepSeek、通义千问、混元等国内外主流模型）。
- **守护进程**：执行 `openclaw daemon install` 和 `openclaw daemon start`，让 OpenClaw 在后台常驻，即使关闭电脑终端，它也会 24 小时在线值班。

### Step 2：多 Agent 分工（获客团队搭建）

在单 BOT（主控 Agent）无法满足复杂获客任务时，OpenClaw 通过 `openclaw agents add` 命令，能快速创建专属 AI 助手军团。多 Agent 的核心优势在于独立工作区、独立权限和独立记忆，从根本上解决了上下文污染和记忆串扰的问题。根据获客岗位，一个最小可用团队通常包含：

1. **主控 Agent（项目经理）**：主控 Agent 本身一直存在，是整个获客系统的“大脑”，负责统一接收各渠道客户消息，并分发到专家 Agent 执行。
2. **线索获取 Agent**：从各渠道抓取新线索，由主控 Agent 直接完成。
3. **意向识别 Agent**：由主控 Agent 调用来分析客户消息，判断意向（高/中/低）。
4. **跟进话术 Agent**：针对不同客户类型，提供个性化回复话术草案，由主控 Agent 最终执行。

以下是获客团队多 Agent 分工的详细操作步骤。

------

## 📋 准备工作

在开始配置前，请确认：

- OpenClaw 已部署并运行（腾讯云/阿里云一键部署，或本地安装）
- 已完成飞书/企微应用配置，能够与 OpenClaw 进行私聊对话
- 拥有飞书群组创建权限
- 记录好飞书机器人的 App ID 和 App Secret

配置过程中，禁止手动修改 openclaw.json 结构或手动创建 BOOTSTRAP.md 文件。OpenClaw 会报错“bootstrapping in progress”，AI 会卡在初始化阶段无法正常响应。

## 🦞 第一步：创建获客 Agent 军团

使用 `openclaw agents add` 命令创建 Agent，系统会自动生成独立的工作目录、配置文件并初始化环境。**禁止手动创建配置文件目录。**

text

```
# 1. 创建线索识别 Agent
openclaw agents add lead-scorer --workspace ~/.openclaw/workspace-lead-scorer

# 2. 创建话术生成 Agent  
openclaw agents add follow-up --workspace ~/.openclaw/workspace-follow-up

# 3. 验证 Agent 是否创建成功
openclaw agents list
```



每个 Agent 获得独立隔离边界，互不干扰且可协同。

创建成功后，每个 Agent 工作区会生成以下核心文件：

| 文件      | 功能                             |
| :-------- | :------------------------------- |
| SOUL.md   | 核心脑区，定义人格、语气、价值观 |
| AGENTS.md | 工作流程编排，定义行为规则       |
| USER.md   | 交互网关，定义触发词与交互方式   |
| TOOLS.md  | 物理抓手，定义工具权限           |

## 🧠 第二步：编写 Agent 配置文件

### 2.1 SOUL.md —— 核心脑区（人格与能力定义）

编辑 `~/.openclaw/workspace-lead-scorer/SOUL.md`：

markdown

```
# 角色：资深销售线索评估师

## 职责
- 分析客户消息内容，判断购买意向等级（S/A/B/C级）
- 输出等级、命中关键词、一句话跟进建议（JSON格式）

## 等级判定规则
- **S级**：直接询问“怎么买”、“多少钱”、“今天能发货吗”
- **A级**：询问功能参数或售后服务（“支持试用吗”、“保修几年”）
- **B级**：初步咨询（“这个产品是什么”、“有人用过吗”）
- **C级**：表情、乱码、广告或不相关信息

## 输出格式（严格遵守JSON）
{"level":"S/A/B/C", "keywords":["关键词1","关键词2"], "suggestion":"跟进话术"}
```



编辑 `~/.openclaw/workspace-follow-up/SOUL.md`：

markdown

```
# 角色：资深销售话术撰写专家

## 职责
- 根据线索等级和客户问题，生成针对性回复话术
- **S级**：热情直接，提供购买指引（“私信我获取专属优惠链接”）
- **A级**：专业耐心，解答疑问并引导试用
- **B级**：友好有信息量，主动提问挖掘需求

## 风格
亲和力强，能有效推动客户转化
```



### 2.2 AGENTS.md —— 工作流程编排

编辑 `~/.openclaw/workspace-lead-scorer/AGENTS.md`，定义触发方式：

markdown

```
# Agent: 线索识别 Agent

description: 分析客户消息的购买意向并打标

goals:
- 准确识别客户购买意图
- 输出标准化的等级评估结果

workflow:
- 接收客户消息
- 按等级判定规则匹配
- 输出JSON格式评估结果

constraints:
- 必须用JSON格式输出
- 不虚构不编造，仅基于客户消息判断
```



## 🔗 第三步：绑定消息路由（Bindings）

OpenClaw 的路由遵循“最具体规则优先”原则，匹配顺序从高到低：

1. 精确到某个用户或群（peer 匹配）
2. 精确到账号（accountId）
3. 精确到频道（channel）
4. 兜底到默认 Agent

**获取群组会话 ID**：打开飞书群组 → 右上角“...” → 设置 → 拉到最底端 → 复制会话 ID（格式如 oc_5b6799cff4a754c15e5ff3025becc648）

编辑 `~/.openclaw/openclaw.json`，添加 bindings 配置：

json

```
{
  "channels": {
    "feishu": {
      "enabled": true,
      "accounts": {
        "feishu-sales": { "appId": "你的App ID", "appSecret": "你的App Secret" },
        "feishu-support": { "appId": "你的App ID", "appSecret": "你的App Secret" }
      }
    }
  },
  "agents": {
    "list": [
      { "id": "main", "workspace": "~/.openclaw/workspace-main" },
      { "id": "lead-scorer", "workspace": "~/.openclaw/workspace-lead-scorer" },
      { "id": "follow-up", "workspace": "~/.openclaw/workspace-follow-up" }
    ]
  },
  "bindings": [
    {
      "agentId": "lead-scorer",
      "match": { "channel": "feishu", "accountId": "feishu-sales", "peer": { "kind": "group", "id": "你的群组会话ID" } }
    },
    {
      "agentId": "follow-up",
      "match": { "channel": "feishu", "accountId": "feishu-support", "peer": { "kind": "group", "id": "你的群组会话ID" } }
    }
  ]
}
```



多条 binding 同时匹配时，**配置文件里靠前的优先**，精确规则必须写在前面。

## 🤝 第四步：Agent 间协同通讯

Multi-Agent 的价值在于协作而非各管各的。OpenClaw 通过内置的 `sessions_send` 工具实现 Agent 直接传话。`sessions_send` 支持两种模式：

- **等待模式**：需要获取对方处理结果，设置 `timeoutSeconds=30`
- **即发即走模式**：不关心结果，设置 `timeoutSeconds=0`

**开启 Agent 间通讯**：

在 `openclaw.json` 中添加配置：

json

```
{
  "tools": {
    "agentToAgent": {
      "enabled": true,
      "allow": ["main", "lead-scorer", "follow-up"]
    }
  }
}
```



此功能默认关闭，必须在配置中显式启用。没开启时，Agent 发消息会被静默拒绝。系统内置防死循环机制：两个 Agent 互发消息，**最多 5 轮自动停止**。

## 🔒 第五步：安全与权限控制

在 `openclaw.json` 中为每个 Agent 配置沙箱和工具权限：

json

```
{
  "agents": {
    "list": [
      { 
        "id": "lead-scorer", 
        "workspace": "~/.openclaw/workspace-lead-scorer",
        "sandbox": { "mode": "all" },
        "tools": { "allow": ["read"], "deny": ["exec", "write", "edit"] }
      },
      { 
        "id": "follow-up", 
        "workspace": "~/.openclaw/workspace-follow-up",
        "sandbox": { "mode": "all" },
        "tools": { "allow": ["read", "write"], "deny": ["exec"] }
      }
    ]
  }
}
```



工具权限优先级：**`deny` 规则优先级高于 `allow`**。即使显式允许了某类操作，如果同时也拒绝它，最终结果是被拒绝。配置时这条铁律必须牢记。

## ✅ 第六步：验证与测试

配置完成后执行以下命令验证：

text

```
# 1. 重启网关使配置生效
openclaw gateway restart

# 2. 查看所有 Agent 及其绑定的路由规则
openclaw agents list --bindings

# 3. 查看实时日志排查问题
openclaw logs --tail 50
```



日志输出包含关键信息：入站消息来源、匹配到的 binding、路由到的 Agent ID、执行结果。消息路由错误或 Agent 不响应时，从这里能看到完整链路。

**测试步骤**：

1. 向绑定的飞书群发送消息：“这个产品多少钱？”
2. `lead-scorer` Agent 自动响应，返回 JSON 格式的等级评估
3. 验证 `follow-up` Agent 能正确生成话术草案

## 🚀 第七步：常见问题与运维

| 问题现象                     | 排查方向                                         | 解决方案                                     |
| :--------------------------- | :----------------------------------------------- | :------------------------------------------- |
| Bot 不响应                   | 检查飞书后台是否添加事件、开通 im:message 权限   | 补全权限后重新绑定                           |
| openclaw doctor 报错         | main Agent 被覆盖或手动修改配置                  | 通过 agents add 恢复，勿手工改 openclaw.json |
| Agent 一直显示 bootstrapping | 手动创建了 BOOTSTRAP.md                          | 删除该文件，重启网关                         |
| 会话混乱/认证失败            | 不同 Agent 共用 agentDir                         | 每个 Agent 使用独立目录                      |
| Agent 间无法通讯             | agentToAgent.enabled 未开启或 allow 列表不含对方 | 开启配置，将双方 ID 加入 allow 列表          |
| 消息路由到错误的 Agent       | bindings 中 accountId 未指定，或精确规则写在后面 | 补全 accountId，将精确规则移到前面           |

配置完成后，`lead-scorer` Agent 自动分析客户意向，`follow-up` Agent 按等级输出针对性话术，主控 Agent 指挥整套流程协作——一个 7×24 小时自动运转的获客 AI 团队就正式搭建完毕了。



### Step 3：连接获客渠道

OpenClaw 原生支持飞书、企业微信、钉钉、QQ、WhatsApp、Telegram 等多种通信平台，客户能在他们最习惯的 IM 应用里和你的“AI 员工”直接交流。

以企业微信为例的标准配置流程：

1. **配置模型与通道**：登录云服务器控制台 → 进入 OpenClaw 应用管理页 → 在“通道配置”中选择“企微机器人（长连接）”。
2. **填写凭证**：粘贴在企业微信后台创建的机器人 App ID 和 Secret。
3. **对话测试**：@你的机器人，输入测试消息，验证其能否正确响应。

### Step 4：开发获客 Skill（核心能力）

当标准问答能力不足时，需要开发自定义 Skill。OpenClaw 的 Skill 是其核心扩展能力，本质是封装好、可复用的任务单元，可将任意业务动作变成“技能”，让 AI 智能体通过自然语言命令直接调用。

一个完整的 Skill 目录结构包括：必选的 `SKILL.md` 核心定义文件，以及可选的 `scripts/`（执行脚本目录，支持 `sh`、`py`、`js` 等）和 `assets/` 静态资源目录。

**客户分层 Skill 开发示例（简化版）**

yaml

```
---
name: customer-scoring
description: 分析客户聊天记录，按意向分级并打标签。意向高者标记为 A，并输出跟进话术。
---

# 使用说明
触发词：“分析这个客户”、“给这个客户打分”。

# 等级定义（示例）
- A 级意向：提到具体产品规格、“多少钱”、“怎么下单”。
- B 级意向：简单咨询、已回复信息但不深入。
- C 级意向：已读未回、仅打招呼。

# 输出格式
等级：[A/B/C]
跟进建议：[下一步动作与话术示例]
```



这个文本配置完成后，Agent 即可理解“给这个客户打分”的自然语言指令，并据此进行分析。实际部署时，还需在 `scripts/` 目录中添加具体的 Python 脚本，实现从消息中抓取关键词、调用判定逻辑、修改 CRM 标签等自动化操作。

------

## 三、打通“获客→转化→复购”完整链路

做好一个核心的获客智能体后，整个链条往往要扩展到后面的转化和服务环节。OpenClaw 的“多 Agent + 技能”体系允许很方便地将它延展成一个完整的、自动化运转的端到端业务系统，链路如下：

- **获取线索 → 意向识别**：由上文**Step 2**中的主控 Agent 和意向识别 Agent 配合完成。
- **智能跟单 → 促成转化**：由**跟进话术 Agent** 生成个性化回复，主控 Agent 发送并记录。高阶场景中，可使用 SaleSmartly 等 Skill，基于真实聊天记录自动打上“高意向-B端”标签并分配给对应销售。
- **服务支持 → 售后关怀 → 复购引导**：由客服 Agent 接入产品知识库处理常见问题，在解决售后问题的同时，推荐互补商品（例如处理完尺寸咨询后推荐配件）。对于老客户，可通过定制 Skill 调取购买历史，在重要节点主动发送关怀消息与复购优惠。

------

## 四、安全风险与防范

OpenClaw 持有很高的系统权限（可访问文件、发消息、调用外部 API），企业级使用必须严格注意几点：

- **禁用公网暴露的 Gateway**：`openclaw.json` 中设置 `gateway.bind: "127.0.0.1"`，禁止对外开放。
- **沙箱隔离**：不同 Agent 分配独立的工作区目录，通过 `tools.deny` 限制高危命令。
- **凭证安全**：API Key 和数据库密码使用环境变量注入，绝对禁止硬编码在脚本中。
- **审计日志**：使用 `openclaw logs` 定期审查所有命令调用和文件操作记录。

------

## 📚 参考资源

1. **官方中文文档**：https://docs.openclaw.ai/zh-CN —— 部署、配置和 Skill 开发官方指南。
2. **智能获客实战**：《实战源码级解读：矩阵跃动龙虾机器人本地AI智能体部署，实现7×24小时无人值守获客》。
3. **客户分层与话术优化**：《OpenClaw实战指南 027-销售与客服：把流量自动转化为订单（微信私域AI打单）》。
4. **多Agent军团搭建**：《一行命令打造多龙虾Agent军团！阿里云/本地部署+多Agent+百炼api配置实战指南》。
5. **多渠道消息接入**：《OpenClaw 企业导入完整指南》与《云上OpenClaw快速接入企业微信指南》。
6. **上下游链路打通**：《OpenClaw enterprise practice - From AI customer service to automated sales》与《跨境聊单太废人力？教你用 OpenClaw + SaleSmartly 提取高意向客户并自动跟进》。
7. **自定义 Skill 开发**：《OpenClaw Application Development - Custom Skills and Plugin Development Guide》和《OpenClaw Skill开发实战从入门到精通》。